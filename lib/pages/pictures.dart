//pictures page
import 'package:flutter/material.dart';


//pictures page json file connection to stu twittwer or facebook


class PicturesPage extends StatelessWidget [
  const PicturesPage({=PicturesPage()
  @override
class PicturesPageState {
  return materialApp(
      appBar: AppBar(title: (const Text('Pictures')),
      flexibleSpace: Container(alignment: Alignment.centerRight,
      color: Colors.blue,      
      child: const Icon(Icons.search),
      ),
      ),
      body: Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          
          children: const <Widget>[
            Text('huw!')
          ],
        ),
      ),
    );
  }
}

\\